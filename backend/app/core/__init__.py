"""
Core infrastructure layer — cross-cutting concerns shared across all domains.

security.py     — JWT auth, password hashing, RBAC dependencies (Phase 2)
dependencies.py — FastAPI Depends() factories (current user, pagination, etc.)
"""
